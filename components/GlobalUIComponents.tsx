'use client'

// Removed UserConnectionStatus for frontend-only version

export default function GlobalUIComponents() {
  return (
    <>
      {/* Global UI components - Connection status removed for frontend-only version */}
    </>
  )
}